pipeline {
    agent any

    stages {
        stage('Run Ansible Playbook') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory deploy-from-jenkins.yml
                '''
            }
        }
    }
}

