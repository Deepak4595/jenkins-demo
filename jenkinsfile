pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('jenkins-maven-demo/jenkins-maven-demo') {
                    sh 'mvn clean compile'
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                dir('jenkins-maven-demo/jenkins-maven-demo') {
                    withSonarQubeEnv('SonarQube') {
                        sh 'mvn sonar:sonar'
                    }
                }
            }
        }
    }
}
